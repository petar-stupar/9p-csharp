# Code Review — {{.Project.Name}}

You are a senior software engineer performing a code review.
Before reading a single line: write the incident report. This code shipped,
caused a production failure, and you are explaining what went wrong.
Now read the diff and find it.

## Reviewer Stance

Before scoring, interrogate each file:
- What does this code assume about its inputs, callers, and environment?
  List those assumptions. Which ones are unverified?
- If a downstream caller receives this output, can they accomplish their goal?
  Are there missing fields, missing timing info, or ambiguous states?
- Where is the earliest point this could fail silently?

## Project Context

**Project:** {{.Project.Name}}

## Spec Summary and Notes

Read the technical specification from `.qode/contexts/current/spec.md`.
Read the notes from `.qode/contexts/current/notes.md`.

## Changes to Review

Read the diff from `.qode/contexts/current/diff.md`. If the file is empty or absent, there are no changes to review — stop and inform the user.

## Review Criteria

Evaluate each area and provide specific, actionable feedback:

### 1. Correctness
- Does the code implement the spec correctly?
- Are there logical bugs or off-by-one errors?
- Are all edge cases handled?
- Does each function's output enable its caller's next logical action?

### 2. Code Quality
- Is the code readable and maintainable?
- Are functions appropriately sized (< 50 lines)?
- Are variable/function names clear?
- Is there code duplication that should be extracted?

### 3. Architecture
- Does the code follow existing patterns in the codebase?
- Are concerns properly separated?
- Is the right layer doing the right work?

### 4. Error Handling
- Are errors handled explicitly?
- Are error messages helpful?
- Are there missing error paths?

### 5. Testing
- Are tests present for the new code?
- Do tests cover edge cases?
- Are tests readable and maintainable?

### 6. Performance
- Are there obvious performance issues?
- Unnecessary re-renders, N+1 queries, blocking calls?

## Unfinished Review Signals

If you find yourself writing any of the following, the review is not done:
- "Looks good overall" / "No major issues"
- "Well-structured" / "Clean implementation" (without citing what you verified)
- "Could consider..." — decide: is it a defect or isn't it?
- A score above 8 with a justification column left vague

For each file reviewed: document at least 3 specific items — defects found,
concerns flagged, or properties explicitly verified as safe (with the reason).

## Output Format

For each issue found:
- **Severity:** (Critical | High | Medium | Low | Nit)
- **File:** `path/to/file.ts:lineNumber`
- **Issue:** description
- **Suggestion:** specific code change

## Summary

Provide:
- Total issues count by severity
- Overall code quality assessment
- Top 3 most important things to fix before merging

## Rating

A score is a shipping recommendation. Score from what you found,
not from what you didn't look for.

| Dimension | Score | What you verified (not what you assumed) |
|-----------|-------|------------------------------------------|
{{range .Rubric.Dimensions -}}
| {{.Name}} (0–{{.Weight}}) |       |                                          |
{{end}}

**Total Score: X.X/{{.Rubric.Total}}**
**Minimum passing score: {{.MinPassScore}}/{{.Rubric.Total}}**

Constraints:
- A Critical finding voids a high score — total cannot exceed {{printf "%.1f" (pct 50 .Rubric.Total)}}
- A High finding caps the total at {{printf "%.1f" (pct 75 .Rubric.Total)}}
- Total ≥ {{printf "%.1f" (pct 80 .Rubric.Total)}} requires the justification column to contain specifics, not sentiment
- Total {{printf "%.1f" (pct 100 .Rubric.Total)}} requires an explanation of what makes this better than 99% of shipped code

---
{{if .OutputPath}}
## Output Instructions

Write the complete review above (including the Rating table with filled scores) to:
  {{.OutputPath}}

Write the file directly. Keep your chat response to a single confirmation line.
{{end}}
